import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    // Marketing-only — no rewrites to the app. App lives at app.gensend.ai
    // and is served by a separate Vercel project.
};

export default nextConfig;
